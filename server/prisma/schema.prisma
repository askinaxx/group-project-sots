generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
}

model Domain {
  id            Int             @id @default(autoincrement())
  domainName    String          @unique @db.VarChar(255)
  registrar     String?         @db.VarChar(255)
  createdAt     String?         @db.VarChar(10)
  updatedAt     String?         @db.VarChar(10)
  expiresAt     String?         @db.VarChar(10)
  daysLeft      Int?
  status        String?         @db.Text
  rdapUrl       String?         @db.VarChar(500)
  lastCheckedAt String?         @db.VarChar(10)
  nameservers   Nameserver[]
  lookups       LookupHistory[]
}

model Nameserver {
  id         Int    @id @default(autoincrement())
  domainId   Int
  nameserver String @db.VarChar(255)
  domain     Domain @relation(fields: [domainId], references: [id], onDelete: Cascade)
}

model LookupHistory {
  id             Int      @id @default(autoincrement())
  domainId       Int?
  domainName     String   @db.VarChar(255)
  queryType      String   @db.VarChar(50)
  responseStatus Int?
  success        Boolean
  checkedAt      String?  @db.VarChar(10)
  errorMessage   String?  @db.Text
  domain         Domain?  @relation(fields: [domainId], references: [id], onDelete: SetNull)
}